const initialState = {
    1: {id: 1, key: '1', name: 'Living Room'},
    2: {id: 2, key: '2', name: 'Kitchen'}
}

export const reducer = (state = initialState, action, rootState) => {
    switch (action.type) {
        case '':
            return state
    }

    return state
}
